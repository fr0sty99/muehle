package main;

import controller.GameController;
import model.BoardModel;
import view.BoardView;

public class Main {
	public static void main(String[] args) {
		GameController gc = new GameController(new BoardView(500, 500), new BoardModel());
		gc.prepareGame();
		gc.runGame();

	}
}
